!SELECTION OF ELEMENTS ATTACHED TO LINES
/PMACRO
!---SAVE CURRENT CONFIGURATION
CM,temp_LINE,LINE
CM,temp_KP,KP
CM,temp_NODE,NODE

lsel,s,p
ksll
nsll
nslk,a

*IF,arg1,EQ,'U',or,ARG1,EQ,'u',THEN
	ESLN,U
*ELSEIF,arg1,EQ,'A',OR,ARG1,EQ,'a',THEN
	ESLN,A
*ELSE
	ESLN
*ENDIF

!---RESUME CONFIGURATION
CMSEL,S,temp_LINE
CMSEL,S,temp_KP
CMSEL,S,temp_NODE

!---DELETE temp CMs
CMDELE,temp_LINE
CMDELE,temp_KP
CMDELE,temp_NODE

/REPLOT
